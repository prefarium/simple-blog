<h1>All tags</h1>

<%# Displays all tags %>
<ul>
  <% @tags.each do |tag| %>
    <li>

      <%= link_to tag.name, tag_path(tag) %>

      <%# Displays amount of articles per tag %>
      <span>
        (<%= pluralize(tag.articles.size, 'article') %>)
      </span>

    </li>
  <% end %>
</ul>
