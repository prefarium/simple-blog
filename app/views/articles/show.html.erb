<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<%= link_to 'Delete',
            article_path(@article),
            method: :delete,
            data: { confirm: 'seriously?' } %>

<%= link_to '<< Back to articles list',
            articles_path,
            class: 'back_to_articles' %>
