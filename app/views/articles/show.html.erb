<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<%= link_to 'Edit',
            edit_article_path(@article) %>

<%= link_to 'Delete',
            article_path(@article),
            method: :delete,
            data: { confirm: 'Are you sure you want it?' } %>

<%= link_to 'Delete (I know what I\'m doing)',
            article_path(@article),
            method: :delete %>

<%= link_to '<< Back to articles list',
            articles_path,
            class: 'back_to_articles' %>
